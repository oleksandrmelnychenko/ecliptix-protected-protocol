<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net9.0;net10.0</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <LangVersion>14</LangVersion>
    <RootNamespace>Ecliptix.Protocol.Client</RootNamespace>

    <!-- Package metadata -->
    <PackageId>Ecliptix.Protocol.Client</PackageId>
    <Version>1.1.0</Version>
    <Authors>Ecliptix</Authors>
    <Company>Ecliptix</Company>
    <Product>Ecliptix Protocol Client</Product>
    <Description>High-performance native Signal Protocol client implementation with X3DH key agreement and Double Ratchet encryption. Features post-quantum hybrid cryptography (Kyber), AES-256-GCM encryption, and secure memory management via libsodium.</Description>
    <Copyright>Copyright (c) 2024-2025 Ecliptix. All rights reserved.</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/oleksandrmelnychenko/Ecliptix.Protocol.System</PackageProjectUrl>
    <RepositoryUrl>https://github.com/oleksandrmelnychenko/Ecliptix.Protocol.System.git</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>signal-protocol;x3dh;double-ratchet;encryption;cryptography;e2ee;kyber;post-quantum;native;interop;client</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>

    <!-- Build configuration -->
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>

    <!-- Signing (optional - enable when .snk file available) -->
    <SignAssembly>false</SignAssembly>
  </PropertyGroup>

  <!-- Package dependencies -->
  <ItemGroup>
    <PackageReference Include="Google.Protobuf" Version="3.33.0" />
    <PackageReference Include="Sodium.Core" Version="1.4.0" />
    <PackageReference Include="Konscious.Security.Cryptography.Argon2" Version="1.3.1" />
    <PackageReference Include="Serilog" Version="4.3.0" />
  </ItemGroup>

  <!-- Shared C# source files -->
  <ItemGroup>
    <Compile Include="..\shared\src\**\*.cs" Link="%(Filename)%(Extension)" />
  </ItemGroup>

  <!-- Native library runtime assets -->
  <ItemGroup>
    <None Include="runtimes\**\*" Pack="true" PackagePath="runtimes" />
    <None Include="build\**\*" Pack="true" PackagePath="build" />
    <None Include="build\**\*" Pack="true" PackagePath="buildTransitive" />
    <None Include="README.md" Pack="true" PackagePath="" />
  </ItemGroup>

</Project>
